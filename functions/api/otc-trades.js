// Reviewed Discord screenshots: desk 1,235, less 11 permanent test/outlier exclusions = 1,224 public trades.
const reviewedTradesSep16 = [
  {
    "timestamp": "2026-09-16T12:55:00Z",
    "side": "buy",
    "zkasAmount": 300000,
    "totalKas": 13783.782
  },
  {
    "timestamp": "2026-09-16T10:58:00Z",
    "side": "sell",
    "zkasAmount": 2000,
    "totalKas": 128
  },
  {
    "timestamp": "2026-09-16T08:58:00Z",
    "side": "sell",
    "zkasAmount": 42000,
    "totalKas": 2730
  },
  {
    "timestamp": "2026-09-16T07:58:00Z",
    "side": "buy",
    "zkasAmount": 2550,
    "totalKas": 116.935707
  },
  {
    "timestamp": "2026-09-16T03:58:00Z",
    "side": "buy",
    "zkasAmount": 15000,
    "totalKas": 630
  },
  {
    "timestamp": "2026-09-16T03:56:00Z",
    "side": "sell",
    "zkasAmount": 170000,
    "totalKas": 10450.0003
  },
  {
    "timestamp": "2026-09-16T02:58:00Z",
    "side": "sell",
    "zkasAmount": 10000,
    "totalKas": 999
  },
  {
    "timestamp": "2026-09-16T02:56:00Z",
    "side": "sell",
    "zkasAmount": 8000,
    "totalKas": 532
  },
  {
    "timestamp": "2026-09-16T02:54:00Z",
    "side": "sell",
    "zkasAmount": 200,
    "totalKas": 13
  },
  {
    "timestamp": "2026-09-16T01:58:00Z",
    "side": "sell",
    "zkasAmount": 30000,
    "totalKas": 1900.0002
  },
  {
    "timestamp": "2026-09-16T00:58:00Z",
    "side": "sell",
    "zkasAmount": 180000,
    "totalKas": 10500.0012
  },
  {
    "timestamp": "2026-09-15T21:58:00Z",
    "side": "sell",
    "zkasAmount": 8000,
    "totalKas": 483.36848
  },
  {
    "timestamp": "2026-09-15T20:58:00Z",
    "side": "buy",
    "zkasAmount": 100517,
    "totalKas": 4152.93825826
  },
  {
    "timestamp": "2026-09-15T20:56:00Z",
    "side": "buy",
    "zkasAmount": 93000,
    "totalKas": 3842.36754
  },
  {
    "timestamp": "2026-09-15T20:54:00Z",
    "side": "buy",
    "zkasAmount": 57000,
    "totalKas": 2359.9995
  },
  {
    "timestamp": "2026-09-15T19:58:00Z",
    "side": "sell",
    "zkasAmount": 100000,
    "totalKas": 7000
  },
  {
    "timestamp": "2026-09-15T18:58:00Z",
    "side": "buy",
    "zkasAmount": 25190,
    "totalKas": 1040.7444982
  },
  {
    "timestamp": "2026-09-15T18:56:00Z",
    "side": "buy",
    "zkasAmount": 10000,
    "totalKas": 390
  },
  {
    "timestamp": "2026-09-15T18:54:00Z",
    "side": "buy",
    "zkasAmount": 50000,
    "totalKas": 2000
  },
  {
    "timestamp": "2026-09-15T18:52:00Z",
    "side": "buy",
    "zkasAmount": 22500,
    "totalKas": 949.99995
  },
  {
    "timestamp": "2026-09-15T18:50:00Z",
    "side": "buy",
    "zkasAmount": 40310,
    "totalKas": 1773.64
  },
  {
    "timestamp": "2026-09-15T17:58:00Z",
    "side": "buy",
    "zkasAmount": 9690,
    "totalKas": 426.36
  },
  {
    "timestamp": "2026-09-15T17:56:00Z",
    "side": "buy",
    "zkasAmount": 10000,
    "totalKas": 456
  },
  {
    "timestamp": "2026-09-15T17:54:00Z",
    "side": "buy",
    "zkasAmount": 158310,
    "totalKas": 7363.2545622
  },
  {
    "timestamp": "2026-09-15T17:52:00Z",
    "side": "buy",
    "zkasAmount": 56689.8,
    "totalKas": 2636.73443547
  },
  {
    "timestamp": "2026-09-15T17:50:00Z",
    "side": "buy",
    "zkasAmount": 43310.2,
    "totalKas": 2165.51
  },
  {
    "timestamp": "2026-09-15T17:48:00Z",
    "side": "buy",
    "zkasAmount": 20000,
    "totalKas": 1000
  },
  {
    "timestamp": "2026-09-15T17:46:00Z",
    "side": "buy",
    "zkasAmount": 86689.8,
    "totalKas": 4334.49
  },
  {
    "timestamp": "2026-09-15T17:44:00Z",
    "side": "buy",
    "zkasAmount": 7324,
    "totalKas": 388.172
  },
  {
    "timestamp": "2026-09-15T17:42:00Z",
    "side": "buy",
    "zkasAmount": 70000,
    "totalKas": 3500
  },
  {
    "timestamp": "2026-09-15T17:40:00Z",
    "side": "buy",
    "zkasAmount": 80000,
    "totalKas": 4240
  },
  {
    "timestamp": "2026-09-15T17:38:00Z",
    "side": "buy",
    "zkasAmount": 100730,
    "totalKas": 5338.69
  },
  {
    "timestamp": "2026-09-15T17:36:00Z",
    "side": "buy",
    "zkasAmount": 500000,
    "totalKas": 26500
  },
  {
    "timestamp": "2026-09-15T17:34:00Z",
    "side": "buy",
    "zkasAmount": 110256,
    "totalKas": 5843.568
  },
  {
    "timestamp": "2026-09-15T16:58:00Z",
    "side": "buy",
    "zkasAmount": 75690,
    "totalKas": 4011.57
  },
  {
    "timestamp": "2026-09-15T16:56:00Z",
    "side": "buy",
    "zkasAmount": 93310,
    "totalKas": 4967.0713883
  },
  {
    "timestamp": "2026-09-15T16:54:00Z",
    "side": "buy",
    "zkasAmount": 198000,
    "totalKas": 10539.92214
  },
  {
    "timestamp": "2026-09-15T16:52:00Z",
    "side": "sell",
    "zkasAmount": 100000,
    "totalKas": 6500
  },
  {
    "timestamp": "2026-09-15T15:58:00Z",
    "side": "buy",
    "zkasAmount": 185500,
    "totalKas": 9874.523015
  }
];

const commonTradeArrays = ['trades', 'data', 'results', 'items', 'completedTrades', 'completed_trades'];

function first(record, keys) {
  for (const key of keys) {
    if (record[key] !== undefined && record[key] !== null && record[key] !== '') return record[key];
  }
  return null;
}

function numberish(value) {
  if (typeof value === 'number' && Number.isFinite(value)) return value;
  if (typeof value !== 'string') return null;
  const parsed = Number(value.replace(/[$,\s]/g, ''));
  return Number.isFinite(parsed) ? parsed : null;
}

function timestampish(value) {
  const numeric = numberish(value);
  if (numeric !== null) return numeric < 10_000_000_000 ? numeric * 1000 : numeric;
  if (typeof value !== 'string') return null;
  const parsed = Date.parse(value);
  return Number.isFinite(parsed) ? parsed : null;
}

function sideish(value) {
  if (typeof value !== 'string') return 'unknown';
  const side = value.trim().toLowerCase();
  if (['buy', 'bid', 'buyer', 'bought'].includes(side)) return 'buy';
  if (['sell', 'ask', 'seller', 'sold'].includes(side)) return 'sell';
  return 'unknown';
}

function tradeArray(payload) {
  if (Array.isArray(payload)) return payload;
  if (!payload || typeof payload !== 'object') return null;
  for (const key of commonTradeArrays) {
    if (Array.isArray(payload[key])) return payload[key];
  }
  if (payload.data && typeof payload.data === 'object') {
    for (const key of commonTradeArrays) {
      if (Array.isArray(payload.data[key])) return payload.data[key];
    }
  }
  return null;
}

function normalizeTrade(value) {
  if (!value || typeof value !== 'object' || Array.isArray(value)) return null;
  const zkasAmount = numberish(first(value, ['zkasAmount', 'zkas_amount', 'amount', 'quantity', 'tokenAmount', 'token_amount']));
  let priceKas = numberish(first(value, ['priceKas', 'price_kas', 'price', 'unitPrice', 'unit_price', 'rate']));
  let totalKas = numberish(first(value, ['totalKas', 'total_kas', 'kasAmount', 'kas_amount', 'total', 'notional']));

  if (priceKas === null && totalKas !== null && zkasAmount !== null && zkasAmount !== 0) priceKas = totalKas / zkasAmount;
  if (totalKas === null && priceKas !== null && zkasAmount !== null) totalKas = priceKas * zkasAmount;

  return {
    timestamp: timestampish(first(value, ['timestamp', 'createdAt', 'created_at', 'completedAt', 'completed_at', 'time', 'date'])),
    side: sideish(first(value, ['side', 'type', 'direction', 'action'])),
    zkasAmount,
    priceKas,
    totalKas,
  };
}

function json(body, status = 200, cacheControl = 'private, no-store, max-age=0') {
  return Response.json(body, {
    status,
    headers: {
      'Cache-Control': cacheControl,
      'X-Content-Type-Options': 'nosniff',
    },
  });
}

async function handleGet({ request, env, waitUntil }) {
  const updatedAt = Date.now();
  const endpoint = env.ZKAS_OTC_API_URL;
  if (!endpoint) {
    if (env.OTC_TRADES) {
      let stored = await env.OTC_TRADES.get('trades:v1', 'json');
      if (Array.isArray(stored?.trades) && stored.trades.length === 1185) {
        stored.trades = [...stored.trades, ...reviewedTradesSep16]
          .map(normalizeTrade)
          .filter(Boolean)
          .sort((a, b) => a.timestamp - b.timestamp)
          .slice(-5000);
        stored.updatedAt = updatedAt;
        await env.OTC_TRADES.put('trades:v1', JSON.stringify(stored));
      }
      const trades = Array.isArray(stored?.trades) ? stored.trades.map(normalizeTrade).filter(Boolean).slice(-5000) : [];
      if (trades.length) {
        return json({ schemaVersion: 1, status: 'live', source: 'screenshot-import', updatedAt: stored.updatedAt || updatedAt, trades }, 200, 'public, max-age=10, s-maxage=30, stale-while-revalidate=120');
      }
      return json({
        schemaVersion: 1,
        status: 'awaiting_configuration',
        source: 'screenshot-import',
        updatedAt,
        trades: [],
        message: 'The private screenshot importer is ready for its first reviewed trade.',
      });
    }
    return json({
      schemaVersion: 1,
      status: 'awaiting_configuration',
      source: 'not-configured',
      updatedAt,
      trades: [],
      message: 'The private OTC API endpoint has not been configured yet.',
    });
  }

  const cacheUrl = new URL(request?.url || 'https://zkas.stream/api/otc-trades');
  cacheUrl.search = '';
  const cacheKey = new Request(cacheUrl.toString(), { method: 'GET' });
  const edgeCache = typeof caches !== 'undefined' ? caches.default : null;
  const cached = edgeCache ? await edgeCache.match(cacheKey) : null;
  if (cached) return cached;

  let upstreamUrl;
  try {
    upstreamUrl = new URL(endpoint);
    if (upstreamUrl.protocol !== 'https:') throw new Error('HTTPS required');
  } catch {
    return json({
      schemaVersion: 1,
      status: 'invalid_upstream_response',
      source: 'not-configured',
      updatedAt,
      trades: [],
      message: 'The configured OTC API endpoint is invalid.',
    }, 500);
  }

  const headers = new Headers({ Accept: 'application/json' });
  const accessKey = env.ZKAS_OTC_API_KEY;
  if (accessKey) {
    const headerName = env.ZKAS_OTC_API_HEADER || 'Authorization';
    const configuredPrefix = env.ZKAS_OTC_API_PREFIX === undefined ? 'Bearer' : env.ZKAS_OTC_API_PREFIX;
    const prefix = configuredPrefix && !configuredPrefix.endsWith(' ') ? `${configuredPrefix} ` : configuredPrefix;
    headers.set(headerName, `${prefix}${accessKey}`);
  }

  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), 10_000);
  try {
    const response = await fetch(upstreamUrl, { headers, signal: controller.signal, redirect: 'follow' });
    if (!response.ok) {
      return json({
        schemaVersion: 1,
        status: 'upstream_unavailable',
        source: 'ronnie-api',
        updatedAt,
        trades: [],
        message: `The private OTC service returned status ${response.status}.`,
      }, 502);
    }

    const payload = await response.json();
    const rows = tradeArray(payload);
    if (!rows) {
      return json({
        schemaVersion: 1,
        status: 'invalid_upstream_response',
        source: 'ronnie-api',
        updatedAt,
        trades: [],
        message: 'The OTC API response needs a small field-mapping adjustment.',
      }, 502);
    }

    const trades = rows.map(normalizeTrade).filter(Boolean).slice(-5000);
    const liveResponse = json(
      { schemaVersion: 1, status: 'live', source: 'ronnie-api', updatedAt, trades },
      200,
      'public, max-age=10, s-maxage=30, stale-while-revalidate=120',
    );
    if (edgeCache && waitUntil) waitUntil(edgeCache.put(cacheKey, liveResponse.clone()));
    return liveResponse;
  } catch {
    return json({
      schemaVersion: 1,
      status: 'upstream_unavailable',
      source: 'ronnie-api',
      updatedAt,
      trades: [],
      message: 'The private OTC service could not be reached.',
    }, 502);
  } finally {
    clearTimeout(timeout);
  }
}

export function onRequest(context) {
  if (context.request.method === 'GET') return handleGet(context);
  return json({ error: 'method_not_allowed' }, 405);
}
