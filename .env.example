# Public ZKas explorer REST backend.
VITE_ZKAS_API_BASE=https://api.zkas.info

# Poll interval in milliseconds.
VITE_POLL_MS=5000

# Keep true while designing so the UI still renders if the API is unreachable.
# Demo data is always labeled clearly in the UI.
VITE_DEMO_FALLBACK=true
